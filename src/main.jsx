import React from 'react';
import ReactDOM from 'react-dom/client';

import Subscipt from './sample.jsx';
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Subscipt />
  </React.StrictMode>,
)
